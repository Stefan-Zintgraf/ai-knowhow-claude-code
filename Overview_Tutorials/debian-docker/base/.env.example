# =============================================================================
# Docker Build Configuration - Environment Variables Example
# =============================================================================
#
# This file contains example environment variables for building the Debian
# development Docker image.
#
# USAGE:
#   1. Copy this file to .env:
#        copy .env.example .env
#
#   2. Edit .env and set your desired values (credentials, etc.)
#
#   3. Run build-docker-image.bat to build the Docker image
#
# SECURITY NOTE:
#   The .env file is gitignored to keep your credentials out of version control.
#   Never commit the .env file to a repository!
#
# =============================================================================
# Required Variables
# =============================================================================

DOCKER_USERNAME=dev
DOCKER_PASSWORD=dev

# =============================================================================
# Optional Variables
# =============================================================================

# VNC_PASSWORD: VNC server password (optional)
# If not set, DOCKER_PASSWORD will be used as the VNC password
# VNC_PASSWORD=dev


# VPN_NETWORK: VPN network CIDR for container access (optional)
# Set this to your OpenVPN network subnet to enable VPN routing in containers
# Example: VPN_NETWORK=10.8.0.0/24
# Leave empty or unset to disable VPN routing
# VPN_NETWORK=
